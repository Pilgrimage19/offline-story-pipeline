# StoryMemory 数据说明

本文说明当前 `story_mem` 已有什么、每份数据能做什么、哪些内容可以供 Chatbot 使用，以及哪些还只能作为内部材料。

## 1. 当前作品与处理状态

当前已完整处理《流浪地球》：`work_id=wandering_earth`。

- 正文：23,236 个规范化字符；
- 章节：4 个；
- story unit：108 个；
- 完整 LLM 抽取：108 个新鲜结果、0 个降级、0 条 LLM 错误；
- 状态 schema：`story-state@0.2`；
- 事实源版本：由 `units_extracted.jsonl` 的 SHA-256 标识；
- FTS5 与 LanceDB 向量索引均已构建。

这些数字描述当前快照，重新切分或重抽取后会变化；程序应读取各阶段 `meta.json`，不要把数字硬编码到 Chatbot。

## 2. 数据分层

| 层 | 典型文件 | 是否可重建 | 主要用途 |
| --- | --- | --- | --- |
| 原文输入 | `00_raw/` | 否 | 保存作品来源、人工核验和最终回指依据。 |
| 规范化文本 | `01_normalized/text.txt`、`meta.json` | 是 | 统一编码与行号映射。 |
| 切分单元 | `02_segmented/units.jsonl` | 是 | 章节、顺序、原文片段与 provenance。 |
| 抽取事实源 | `03_extracted/units_extracted.jsonl` | 是，但成本较高 | 在线检索、证据读取、状态理解的唯一数据源。 |
| 抽取运行材料 | `03_extracted/cache/`、checkpoint、final state | 是 | 缓存、局部重跑、可重放状态与故障诊断。 |
| 验证产物 | `04_validated/` | 是 | 数据结构与人工抽检依据。 |
| 检索 side index | `05_index/story.db`、`vectors.lance/` | 是 | 加快召回；不能替代事实源。 |

## 3. StoryUnit：最小故事记忆单元

每个 unit 是“有位置的原文片段 + 对该片段的顺序理解”。核心定位字段：

```jsonc
{
  "work_id": "wandering_earth",
  "unit_id": "we-0003",
  "order": 3,
  "chapter_idx": 1,
  "chapter_name": "上篇 刹车时代",
  "start_line": 11,
  "end_line": 18,
  "text": "该片段原文"
}
```

`order` 供防剧透过滤；章节与行号供用户友好的位置描述及原文核验；`unit_id` 用于短期证据引用，但不应作为跨版本唯一锚点。

### LLM 结构化理解字段

| 字段 | 含义 | 当前建议 |
| --- | --- | --- |
| `summary` | 该段发生什么的简要理解 | 仅作为内部推理/召回证据，不直接展示。 |
| `characters` | 抽取到的人物相关标签 | 用于候选召回；标签仍混有组织、群体和关系组合，不能直接生成人物页。 |
| `locations` | 地点/场景标签 | 用于候选召回；需清洗层级与别名后才适合导航。 |
| `key_terms` | 术语、物件、设定词 | 适合检索扩展和术语解释候选。 |
| `entity_updates` | 本段对人物、物件、地点状态的增量 | 内部状态流和后续一致性功能。 |
| `plotline_updates` | 情节线 open/advance/close 增量 | 内部状态流、后续回顾与预测核验。 |
| `context_refs` | 指向前文单元或实体的关联 | 消解指代、关联前文。 |
| `recent_event` | 本段如何推进剧情 | 维持短上下文。 |
| `state_snapshot` | 处理到本段后的故事状态 | 用于重放、验证、内部一致性。 |
| `degraded` | 是否是故障降级结果 | 质量监控；不应把降级理解当作可靠事实。 |

## 4. StoryState：顺序状态而非读者记忆

`state_snapshot`、`checkpoint_chXX.json` 和 `state_final.json` 共同表示“作品读到某一段时，故事内部已知什么”。它包括角色/物件/地点当前状态、开放或关闭的情节线、最近事件和较早背景的压缩 `backdrop`。

它不等于用户的长期记忆：不保存用户偏好、阅读感受、问题或预测。那些内容属于 Chatbot 的 `Notes` 和独立阅读手账。

当前 Chatbot 已使用单元检索和证据读取；状态快照尚未直接展示给读者，后续应先经过准确性、别名与防剧透评估。

## 5. 检索数据与返回证据

`StoryMemory.search()` 返回统一 Evidence：原文、摘要、得分、章节/行号/标签元数据。Chatbot 回答事实问题时应基于已读 Evidence 组织语言，而不是把索引分数或整段结构化字段原样展示。

三条路径的关系：

```text
auto: LanceDB vector → SQLite FTS5 → JSONL scan
vector: LanceDB（不可用就明确失败）
fts: SQLite FTS5 → JSONL scan
```

- FTS5 适合专名、术语和直接关键词；
- 向量适合同义表达和口语化问题；
- scan 是无索引时的保底，不是性能目标。

`get_reading_locations()` 只返回章节标签、顺序范围和行范围，不返回任何剧情摘要，因此可安全用于“我读完上篇”这类进度选择。

`search_with_diagnostics()` 返回 `requested_retrieval`、`used_retrieval`、`fallback_reason`、边界内可用单元数、候选数和事实源版本；它只用于测试、日志和运维，不直接给读者看。

## 6. 当前可以支撑的产品功能

- 只讨论已读范围的名词解释、前因后果与指代澄清；
- 将用户选择的完整章节映射为安全阅读边界；
- 显示“你读到哪里附近”的友好定位；
- 基于已读证据做很短的回顾；
- 将用户的阅读反应、问题、预测锚定在阅读位置（由 Chatbot 存储）。

## 7. 当前不应直接支撑的功能

- 直接展示自动生成的人物百科、关系图、全书总结；
- 用 state 或检索去回答未读后文；
- 将 `summary` 当作替代原文的阅读内容；
- 从标签自动推断用户性格、偏好或情感；
- 在 unit 中途用完整 `order` 边界开放内容。首版只将“完整读完的章节/单元”作为进度；段落中途定位需要更细粒度锚点。

## 8. 数据质量边界

自动验证已证明文本、顺序、章节和行号结构正确；这不保证每一个实体标签或 summary 的语义都无误。产品层应把原文及其位置作为最终事实依据，并在没有已读证据时明确表示无法确认。

新增记忆项的判定原则：章节位置、索引诊断和可由现有字段派生的接口由本仓库直接实现；只有缺少原始文本、必要抽取字段、人工标注或作品元数据时，才向数据提供方提出需求。